# from models import User, Rating, Category
# from Base_connect import session
# from populate_rating import product

# category = Category(category="sample category")
# user = session.query(User).filter(User.id==1).first()
# rating = Rating(value=5, user=user, category=category, product=product)
