# from models import User, Billing_address
# from Base_connect import session
# from populate_users import user

# user = session.query(User).filter(user.id==2).first()

# billing_address = Billing_address(user=user, bill_address='383 Billing lag')

# session.add(billing_address)
# session.commit()
# session.close()